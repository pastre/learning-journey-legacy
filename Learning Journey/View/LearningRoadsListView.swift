import SwiftUI
import Combine

struct LeariningRoadsListView: View {
    
    @Environment(\.injected) private var injected: DIContainer
    
    var body: some View {
        Text("TODO")
    }
    
}
